'use strict';

function create(car) {
  return true;
}

function findAll() {
  return true;
}

function findById(id) {
  return true;
}

function removeById(id) {
  return true;
}

module.exports = {
  create,
  findAll,
  findById,
  removeById,
};
